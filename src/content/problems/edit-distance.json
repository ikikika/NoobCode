{
  "slug": "edit-distance",
  "title": "Edit Distance",
  "difficulty": "hard",
  "tags": [
    "string",
    "dynamic-programming"
  ],
  "patterns": [
    "dynamic-programming"
  ],
  "description": "Given two strings `word1` and `word2`, return the minimum number of operations\n(insert, delete, or replace a character) required to convert `word1` into\n`word2`.",
  "constraints": [
    "0 <= word1.length, word2.length <= 500",
    "Both consist of lowercase English letters."
  ],
  "examples": [
    {
      "input": "word1 = \"horse\", word2 = \"ros\"",
      "output": "3"
    },
    {
      "input": "word1 = \"intention\", word2 = \"execution\"",
      "output": "5"
    }
  ],
  "functionName": {
    "python": "min_distance",
    "javascript": "minDistance",
    "typescript": "minDistance"
  },
  "starterCode": {
    "python": "def min_distance(word1, word2):\n    # Return the edit distance between the words.\n    pass\n",
    "javascript": "function minDistance(word1, word2) {\n  // Return the edit distance between the words.\n}\n",
    "typescript": "function minDistance(word1: string, word2: string): number {\n  // Return the edit distance between the words.\n  return 0;\n}\n"
  },
  "tests": [
    {
      "name": "example 1",
      "args": [
        "horse",
        "ros"
      ],
      "expected": 3
    },
    {
      "name": "example 2",
      "args": [
        "intention",
        "execution"
      ],
      "expected": 5
    },
    {
      "name": "empty source",
      "args": [
        "",
        "abc"
      ],
      "expected": 3
    },
    {
      "name": "equal",
      "args": [
        "abc",
        "abc"
      ],
      "expected": 0,
      "hidden": true
    },
    {
      "name": "both empty",
      "args": [
        "",
        ""
      ],
      "expected": 0,
      "hidden": true
    }
  ],
  "solutions": [
    {
      "approachName": "2D Dynamic Programming",
      "summary": "dp[i][j] is the edit distance between the first i and first j characters.",
      "timeComplexity": "O(m · n)",
      "spaceComplexity": "O(m · n)",
      "technique": {
        "primaryPattern": "dynamic-programming",
        "optimal": true,
        "signature": {
          "maxLoopDepth": 2,
          "usesHashStructure": false,
          "usesSorting": false,
          "usesRecursion": false,
          "twoPointer": false
        }
      },
      "steps": [
        {
          "title": "Initialize the borders",
          "explanation": "Turning a prefix into the empty string costs one delete per character, and vice versa.",
          "code": {
            "python": "def min_distance(word1, word2):\n    m, n = len(word1), len(word2)\n    dp = [[0] * (n + 1) for _ in range(m + 1)]\n    for i in range(m + 1):\n        dp[i][0] = i\n    for j in range(n + 1):\n        dp[0][j] = j\n    return dp[m][n]\n",
            "javascript": "function minDistance(word1, word2) {\n  const m = word1.length, n = word2.length;\n  const dp = Array.from({ length: m + 1 }, () => new Array(n + 1).fill(0));\n  for (let i = 0; i <= m; i++) dp[i][0] = i;\n  for (let j = 0; j <= n; j++) dp[0][j] = j;\n  return dp[m][n];\n}\n",
            "typescript": "function minDistance(word1: string, word2: string): number {\n  const m = word1.length, n = word2.length;\n  const dp = Array.from({ length: m + 1 }, () => new Array<number>(n + 1).fill(0));\n  for (let i = 0; i <= m; i++) dp[i][0] = i;\n  for (let j = 0; j <= n; j++) dp[0][j] = j;\n  return dp[m][n];\n}\n"
          }
        },
        {
          "title": "Fill from smaller subproblems",
          "explanation": "If the characters match, carry the diagonal; otherwise take 1 + the best of delete, insert, or replace.",
          "code": {
            "python": "def min_distance(word1, word2):\n    m, n = len(word1), len(word2)\n    dp = [[0] * (n + 1) for _ in range(m + 1)]\n    for i in range(m + 1):\n        dp[i][0] = i\n    for j in range(n + 1):\n        dp[0][j] = j\n    for i in range(1, m + 1):\n        for j in range(1, n + 1):\n            if word1[i - 1] == word2[j - 1]:\n                dp[i][j] = dp[i - 1][j - 1]\n            else:\n                dp[i][j] = 1 + min(dp[i - 1][j], dp[i][j - 1], dp[i - 1][j - 1])\n    return dp[m][n]\n",
            "javascript": "function minDistance(word1, word2) {\n  const m = word1.length, n = word2.length;\n  const dp = Array.from({ length: m + 1 }, () => new Array(n + 1).fill(0));\n  for (let i = 0; i <= m; i++) dp[i][0] = i;\n  for (let j = 0; j <= n; j++) dp[0][j] = j;\n  for (let i = 1; i <= m; i++) {\n    for (let j = 1; j <= n; j++) {\n      if (word1[i - 1] === word2[j - 1]) {\n        dp[i][j] = dp[i - 1][j - 1];\n      } else {\n        dp[i][j] = 1 + Math.min(dp[i - 1][j], dp[i][j - 1], dp[i - 1][j - 1]);\n      }\n    }\n  }\n  return dp[m][n];\n}\n",
            "typescript": "function minDistance(word1: string, word2: string): number {\n  const m = word1.length, n = word2.length;\n  const dp = Array.from({ length: m + 1 }, () => new Array<number>(n + 1).fill(0));\n  for (let i = 0; i <= m; i++) dp[i][0] = i;\n  for (let j = 0; j <= n; j++) dp[0][j] = j;\n  for (let i = 1; i <= m; i++) {\n    for (let j = 1; j <= n; j++) {\n      if (word1[i - 1] === word2[j - 1]) {\n        dp[i][j] = dp[i - 1][j - 1];\n      } else {\n        dp[i][j] = 1 + Math.min(dp[i - 1][j], dp[i][j - 1], dp[i - 1][j - 1]);\n      }\n    }\n  }\n  return dp[m][n];\n}\n"
          }
        }
      ]
    }
  ]
}

{
  "slug": "string-reversal",
  "title": "String Reversal",
  "difficulty": "easy",
  "tags": [],
  "patterns": [
    "brute-force"
  ],
  "description": "Given a string, return a new string with the reversed order of characters.",
  "constraints": [],
  "examples": [
    {
      "input": "'apple'",
      "output": "'leppa'",
      "explanation": "The reversed string of 'apple' is 'leppa'."
    },
    {
      "input": "'hello'",
      "output": "'olleh'",
      "explanation": "The reversed string of 'hello' is 'olleh'."
    },
    {
      "input": "'Greetings!'",
      "output": "'!sgniteerG'",
      "explanation": "The reversed string of 'Greetings!' is '!sgniteerG'."
    }
  ],
  "functionName": {
    "python": "reverse_string",
    "javascript": "reverseString",
    "typescript": "reverseString"
  },
  "starterCode": {
    "python": "def reverse_string(string):\n    # Return the reversed string.\n    pass\n",
    "javascript": "function reverseString(string) {\n  // Return the reversed string.\n}\n",
    "typescript": "function reverseString(string: string): string {\n  // Return the reversed string.\n  return '';\n}\n"
  },
  "tests": [
    {
      "name": "reverses a string",
      "args": [
        "abcd"
      ],
      "expected": "dcba"
    },
    {
      "name": "reverses a string with leading spaces",
      "args": [
        "  abcd"
      ],
      "expected": "dcba  "
    }
  ],
  "solutions": [
    {
      "approachName": "Split / reverse / join",
      "summary": "Split into characters, reverse the array, then join back into a string.",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(n)",
      "technique": {
        "primaryPattern": "brute-force",
        "optimal": true,
        "signature": {
          "maxLoopDepth": 0,
          "usesHashStructure": false,
          "usesSorting": false,
          "usesRecursion": false,
          "twoPointer": false
        }
      },
      "steps": {
        "typescript": [
          {
            "title": "Start from the function shape",
            "explanation": "",
            "code": "function reverseString(string: string): string {\n  return '';\n}"
          },
          {
            "title": "Plan",
            "explanation": "",
            "code": "function reverseString(string: string): string {\n  // Split string into an array of letters.\n\n  // Reverse the order of letters in the Array\n\n  // Join reversed array back into a string (of letters).\n\n  return '';\n}"
          },
          {
            "title": "Split string into letters",
            "explanation": "",
            "code": "function reverseString(string: string): string {\n  // Split string into an array of letters.\n  const letters = string.split('');\n\n  // Reverse the order of letters in the Array\n  \n  // Join reversed array back into a string (of letters).\n\n  return '';\n}"
          },
          {
            "title": "Reverse order",
            "explanation": "",
            "code": "function reverseString(string: string): string {\n  // Split string into an array of letters.\n  const letters = string.split('');\n\n  // Reverse the order of letters in the Array\n  const reversedArray = letters.reverse();\n  \n  // Join reversed array back into a string (of letters).\n\n  return '';\n}"
          },
          {
            "title": "Join reversed array",
            "explanation": "",
            "code": "function reverseString(string: string): string {\n  // Split string into an array of letters.\n  const letters = string.split('');\n\n  // Reverse the order of letters in the Array\n  const reversedArray = letters.reverse();\n\n  // Join reversed array back into a string (of letters).\n  const reversedString = reversedArray.join('');\n  \n  return reversedString;\n}"
          },
          {
            "title": "Bonus",
            "explanation": "",
            "code": "function reverseString(string: string): string {\n  // // Split string into an array of letters.\n  // const letters = string.split('');\n\n  // // Reverse the order of letters in the Array\n  // const reversedArray = letters.reverse();\n\n  // // Join reversed array back into a string (of letters).\n  // const reversedString = reversedArray.join('');\n  \n  // return reversedString;\n\n  // bonus: we can actually combine the steps into one line\n}"
          },
          {
            "title": "Combine steps",
            "explanation": "",
            "code": "function reverseString(string: string): string {\n  // // Split string into an array of letters.\n  // const letters = string.split('');\n\n  // // Reverse the order of letters in the Array\n  // const reversedArray = letters.reverse();\n\n  // // Join reversed array back into a string (of letters).\n  // const reversedString = reversedArray.join('');\n  \n  // return reversedString;\n\n  // bonus: we can actually combine the steps into one line\n  return string.split('').reverse().join('');\n}"
          }
        ]
      }
    },
    {
      "approachName": "For of loop",
      "summary": "Use a for-of loop to iterate over the string and prepend each character to the result.",
      "timeComplexity": "O(n²)",
      "spaceComplexity": "O(n)",
      "technique": {
        "primaryPattern": "brute-force",
        "optimal": false,
        "signature": {
          "maxLoopDepth": 1,
          "usesHashStructure": false,
          "usesSorting": false,
          "usesRecursion": false,
          "twoPointer": false
        }
      },
      "steps": {
        "typescript": [
          {
            "title": "Start from the function shape",
            "explanation": "",
            "code": "function reverseString(string: string): string {\n  return '';\n}"
          },
          {
            "title": "Plan",
            "explanation": "",
            "code": "function reverseString(string: string): string {\n  // define end state\n\n  // use for of loop to break string and iterate over the string\n\n    // prepend each character to the end state\n  \n  return '';\n}"
          },
          {
            "title": "Define end state",
            "explanation": "",
            "code": "function reverseString(string: string): string {\n  // define end state\n  let reversedString = '';\n\n  // use for of loop to break string and iterate over the string\n\n    // prepend each character to the end state\n  \n  return reversedString;\n}"
          },
          {
            "title": "For of loop",
            "explanation": "",
            "code": "function reverseString(string: string): string {\n  // define end state\n  let reversedString = '';\n\n  // use for of loop to break string and iterate over the string\n  for (const char of string) {\n    // prepend each character to the end state\n    \n  }\n  return reversedString;\n}\n\n// avoid classic for loop because it is easy to make mistakes\n// for( var i = 0; i < string.length; i++) \n// A classic indexed loop can go wrong more easily: off-by-one (i <= length), wrong direction when reversing, or var leaking the index. \n// for...of skips indexing, so those mistakes don’t come up."
          },
          {
            "title": "Prepend to end state",
            "explanation": "",
            "code": "function reverseString(string: string): string {\n  // define end state\n  let reversedString = '';\n\n  // use for of loop to break string and iterate over the string\n  for (const char of string) {\n    // prepend each character to the end state\n    reversedString = char + reversedString;\n  }\n  return reversedString;\n}\n\n// avoid classic for loop because it is easy to make mistakes\n// for( var i = 0; i < string.length; i++) \n// A classic indexed loop can go wrong more easily: off-by-one (i <= length), wrong direction when reversing, or var leaking the index. \n// for...of skips indexing, so those mistakes don’t come up."
          }
        ]
      }
    },
    {
      "approachName": "Reduce",
      "summary": "Use a reduce function to iterate over the string and prepend each character to the result.",
      "timeComplexity": "O(n²)",
      "spaceComplexity": "O(n)",
      "technique": {
        "primaryPattern": "brute-force",
        "optimal": false,
        "signature": {
          "maxLoopDepth": 0,
          "usesHashStructure": false,
          "usesSorting": false,
          "usesRecursion": false,
          "twoPointer": false
        }
      },
      "steps": {
        "typescript": [
          {
            "title": "Start from the function shape",
            "explanation": "",
            "code": "function reverseString(string: string): string {\n  return '';\n}"
          },
          {
            "title": "Plan",
            "explanation": "",
            "code": "function reverseString(string: string): string {\n  // split string\n  \n  // apply reduce function\n  \n  return '';\n}\n\n// reduce takes all different values within an array and condense them into a single value\n// reduce( () => {}, initialValue)\n// take initialValue and pass into the arrow function\n// whatever value returned will be used as starting value for every successive run of the arrow function"
          },
          {
            "title": "Split string",
            "explanation": "",
            "code": "function reverseString(string: string): string {\n  // split string\n  return string.split('');\n  // apply reduce function\n}\n\n// reduce takes all different values within an array and condense them into a single value\n// reduce( () => {}, initialValue)\n// take initialValue and pass into the arrow function\n// whatever value returned will be used as starting value for every successive run of the arrow function"
          },
          {
            "title": "Apply reduce",
            "explanation": "",
            "code": "function reverseString(string: string): string {\n  // split string\n  // apply reduce function\n  return string.split('').reduce((reversed, char) => char + reversed, '');\n}\n\n// reduce takes all different values within an array and condense them into a single value\n// reduce( () => {}, initialValue)\n// take initialValue and pass into the arrow function\n// whatever value returned will be used as starting value for every successive run of the arrow function"
          }
        ]
      }
    }
  ]
}
